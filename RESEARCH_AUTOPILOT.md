# 自动研究与修复进度

## 当前接续：V30/V30r1概率预测层（2026-08-30）

用户要求暂停V29并直接建设V30概率预测层；V29已在候选性能输出前中断，冻结锁和中断证据保留，禁止重启或改写。V30已用真实PIT数据完成2019–2025 Purged Walk-Forward并生成不可变最新预测，但认证为有效负结果。诊断发现无约束Platt负斜率会反转跨期排序；父V30结果锁`c7b69369d0d72e47156c5b66881df535c9b7331cbd5374a66e48512d381888f8`不得修改。

独立V30r1只修非负单调校准和基于过去OOS的模型冠军选择，计划锁`47927f621c07695752a98e6830dacee6a56c1f3aa6f515303eddd315dd0b0174`、结果锁`c631c3293859dd9bc61a38a61959f4bdfbf2e83464e54031899838834dac7a75`均完整。修复生效但`production_prediction_ready=false`：20日ECE 0.06694，且基线、稳定性、regime、概率质量和成本压力未全通过。`future_126d_confirmed=false`不参与立即认证，`execution_authorized=false`永久保持。

最新不可变前向快照为2026-08-28、261只；可发布修订版SHA-256 `149be61423334f01b4928b59cf9eb9acefccf3df9711d19063011dcbdb7c5ddb`。增量HFQ覆盖当期沪深300成分300/300；冻结8月21日249只及61个V10特征复现通过，最大数值差`4.25e-16`。首个forward版因V6/QFQ辅助排名只覆盖231/261而在输出前失败，锁`7cfbeb82325892adb6a77945f28d0708552d5626fcc1d672aa4fc869d6c720ec`和失败证据保留。独立`forward-r1`固定将缺失的30只辅助排名设为中性0.5，不影响原始/校准概率或预期收益，锁`b7890e39ebfb3965ca676929c896bfb81095ea7b528fba3dda4a02cf6d696a4d`。干净检出审计又发现旧仓库未提交超大OOS明细且历史换行规范化使完整父锁不能在克隆中重验；独立`forward-r2`锁`110fa074f16235ab413e13b788767913c93e04e051c2105c39b462202d384b17`仅绑定可发布且推理必需的模型、校准器、特征画像、认证状态和代码，明确不重新认证完整历史OOS。r2与r1的全部概率、收益、排名和candidate score逐位一致。当前漂移SEVERE、置信度LOW、`production_prediction_ready=false`、`execution_authorized=false`。后续自动任务只能追加新日期预测和结算成熟1/5/20日结果，不得覆盖同日快照。禁止利用已见2019–2025结果搜索参数、阈值、模型权重或门禁；下一次模型假设必须有新PIT信息或新forward证据。V6继续正式排名默认。

## 授权与边界（2026-08-29）

用户授权在本项目内自动检查、修复、测试并继续改进。默认模型仍为 V6；没有券商连接、下单、付费数据购买或向外上传项目数据的授权。

- 保留 V14～V19 的代码、数据、冻结锁、历史报告及失败记录；不通过改锁把失败变成通过。
- 修复在独立版本中进行。性能测试前冻结，测试后发现实现错误则新建修订版并说明原因。
- 不根据反复查看的 2020～2025 年成绩宣称独立样本外成功；不降低原先替换门槛。
- 每次定时续跑最多推进一个有明确假设的实验；同一时间最多一个重型训练。先查进程和运行记录，不重复启动。
- 若只是等待运行完成或缺少未来交易日，不再生成新的调参实验；如实记录等待。
- 不修改既有每日影子任务的冻结输入或写入边界。

## 持续决策闭环（2026-08-29追加授权）

用户要求每完成一项任务就自主决策、给出方案并继续执行，不再等待逐项确认。

- 自动任务 `a` 改为每30分钟唤醒本任务。当前轮内能完成的安全步骤连续执行；后台长任务由后续唤醒检查接续，不声称完成瞬间自动触发。
- 先看真实结果，再区分实现错误、数据问题、有效负结果和通过；每一步的依据、方案、验收条件、实际执行及下一步追加到 `RESEARCH_DECISIONS.md`。
- 优先完成当前版本，不重复启动；正在训练时只做不冲突的轻量检查。只有明确假设才开新实验，不能为维持自动运行而不断刷版本。
- 连续三次无进展应诊断并降频或等待，不反复重试昂贵工作；只有需要新的外部授权、收费、数据授权或用户冲突选择时才提问。
- 原影子观察仍限上海工作日18:30之后当天一次，在此之后的首次唤醒补执行；使用 `artifacts/autopilot/shadow_attempts/YYYY-MM-DD.json` 保存调度尝试记录，避免半小时重复抓取。暴露陈旧只允许原规则的一次刷新重试。
- 执行影子脚本前核对实际写入范围，越过原限定范围时先诊断，不绕过安全检查。

## 当前任务：V25r1时间窗口集成，V24降为后台数据线

V21已于2026-08-29 06:27:54完成，73/73日期父版IC复现通过，14份输出哈希复核通过，冻结输入完整，正式结论仍是keep_v6。分项诊断显示：V13两阶段分数5日IC为-0.04120且仅1/6年度为正；V16混合后为-0.02520；已有global_model_score为+0.03300且5/6年度为正，20日残差IC为+0.03999。科技5日IC仍为-0.01159，不能声称解决科技预测。

据此只预注册一个V22假设：不重训、不调权重，仅把portfolio_score换成冻结的global_model_score，并使用V20r2同一公共日历、现金持仓账、交易限制、成本、换股和代理基准回放。先复现V20r2对照，不一致即失败；只允许这一项替换，不并行加入正文、排名目标、择时或科技门禁。即使结果好也是基于已观察V21证据的回顾性消融，不能替换V6。协议：`artifacts/research_v22/protocol.json`。

V22实现已完成，针对V22、V21和V20r2共36项测试通过；测试收据明确记录在冻结前未读取V22收益。冻结锁为 `315EFE254071DC9EB9A4DD7F2C010557AF22808C90971F4544899F1071151FF2`，冻结校验通过、execution_authorized=false。2026-08-29 10:04上海时间只启动一次隐藏回放，启动器PID34944、实际Python PID38784均已核对命令行和创建时间，当前阶段为loading_dataset_for_frozen_volatility。`artifacts/active_research.json` 已切到V22；只读日志查看器会自动跟随，正式V6不变。下次优先核验真实进程、状态、日志与锁，禁止重复启动；完成后先要求V16控制账本逐项复现，再阅读global_only结果并按全部原始门槛决策。

10:14上海时间V22在构建完2680710行公共日历后、尚未进入任何组合回放或读取收益结果时失败：`parent.mode` 命中了Pandas的 `DataFrame.mode` 方法，而不是名为mode的列。冻结V22不改写，失败状态和日志保留。已建立实现错误修订版V22r1，只把该访问改成 `parent["mode"]`；其余冻结分数、特征、优化器、账本、成本、公司行动和门槛不变。V22r1及父链共38项测试通过，冻结锁 `BEDA5FAD45E9C4B697C01805E48A327B719EB01A30AB2C1CA75C86C28BF10E0A`，10:40上海时间排他启动，启动器PID37776。研究指针已切至V22r1，正式V6不变。下次核验实际Python子进程、状态、日志和冻结锁；禁止重启V22或重复启动V22r1。

V22r1已于10:49:35完成。独立验收确认四份最终输出哈希、六年checkpoint和冻结锁完整；V16控制的73期收益、21898行持仓及1533行每日NAV独立比较差异为0，控制复现通过。global_only历史累计收益26.96%，代理基准30.45%，超额-3.49个百分点，仅3/6年度正超额；最大回撤-40.16%，科技IC -0.01159。虽然全体5日IC +0.03300、20日IC +0.03999，但所有六项替换门槛均失败，分类为有效负结果，保持V6。

下一步不启动新的模型或改权重。只预注册V23冻结事后诊断，固定检查全样本IC为何没有传导至头尾分位与实际超配持仓：按年头尾十分位、实际超配与未选股票、行业贡献、相对V16的换手成本差异。禁止重跑组合、搜索top-N/权重/门槛或反转信号。协议 `artifacts/research_v23/protocol.json`；下次先实现诊断测试和冻结，再执行一次。

V23已实现、42项父链/诊断测试通过并冻结，锁 `9BDFBAAD4A5502D338D3509125596E97783E4320A58FCC44855D6CA4B21C7E36`。诊断未重跑组合：头尾十分位5日/20日价差分别仅+0.077%/+0.277%，且都只有3/6年度为正；实际超配相对同业未选股票的等行业-日期差为-0.096%/-0.023%。科技主动加权目标贡献为负；global相对控制的平均每期成本只多0.000873个百分点，无法解释-3.49个百分点累计超额损失。结论是全样本弱正IC不稳定，且没有传导到冻结组合选中的超配尾部；不能据此事后搜索组合参数。

历史模型线暂不再开新性能实验。下一项已预注册为公告正文V2数据质量：仍只用原12份固定官方PDF，不读任何行情或收益；先绑定“本次预测/前次预测/上年同期”和原币/人民币折算角色，未绑定数字继续隔离，扫描件无独立OCR验证不放行。协议 `artifacts/announcement_body_v2/protocol.json`，model_training_ready仍为false。下次先建立人工冻结金标、解析规则与测试，不并入任何模型。

公告正文V2按PDF流程完整渲染核对4份金标文档共9页，建立22条期间/币种事实。首次冻结执行准确率90.48%、召回率86.36%而失败：大北农修正预告的扁平文本把“本报告期”和“上年同期”表格列交错。失败锁和报告保留；V2r1只改用显式表格单元格，没有改金标或验收门槛。20项父链/修复测试通过，V2r1锁 `a8f108e6b501f1ce2c4ad36a9a01478bb2252f735d5c8530990d46f515ccb001`，最终22/22精确匹配、无缺失或意外事实。

这只证明4份金标上的保守绑定可行，不代表训练数据已就绪：另有7份文本PDF未人工审阅、1份扫描件隔离，历史PIT仍未验证。因此model_training_ready保持false。下一步预注册公告正文V3：渲染并逐页审阅剩余7份文本PDF，扩展到固定试点完整金标；不抓新文档、不启用未经验证OCR、不读行情收益。协议 `artifacts/announcement_body_v3/protocol.json`。

公告正文V3已完成剩余7份文本PDF的22页逐页视觉审阅，固定试点扩展到11份文本、87条人工事实；1份扫描件仍隔离。首次冻结评估匹配77/87，准确率93.90%、召回率88.51%，原因均为明确实现缺陷：跨行表格标签、扣非行误映射、“经审计”措辞、期限句式和小数尾零。V3锁 `a5edd42a8f3a0fdc94aac72ad60ac68b505c0e606fa426c1c6602998608d282b` 与失败证据保留。

V3r1没有改金标、文档或门槛，只修上述5项实现问题；3项修复测试通过，冻结锁 `127fd128803b25adc0f046137f0db199799d3e08688436e42c9938a3e23b0e4b`。冻结后唯一评估87/87精确匹配，准确率/召回率100%，冻结输入完整。该结论只说明固定试点的绑定正确，`historical_pit_verified=false`、`model_training_ready=false`、`execution_authorized=false`仍保持。下一步只做公告历史PIT可用性审计：先核对官方披露时间、查询可追溯性、修订/撤回关系和覆盖分布，不读收益、不训练模型、不抓取无协议的新文档；证据不足就继续隔离。V4协议已预注册在 `artifacts/announcement_body_v4/protocol.json`，尚未实现或运行。

V4已按冻结规则完成966,865条公告元数据审计，锁 `978619bc879718696f681967a56d1eec7bdcb96e07fdd6626d999d48a1414494` 完整。源SHA与行数、必填字段、公告ID无冲突及12/12试点官方身份均通过；但源只从2017-01-03开始，2015–2016四类覆盖全部为空。21,725条修正/补充/撤回/终止类标题没有显式父公告字段，且历史元数据没有当时首次见证时间；12份试点均到2026-08-29才首次本地见证。结论为有效数据限制：`historical_pit_verified=false`、`model_training_ready=false`，不能通过改解析器或降低门槛解决。

下一步不再伪造历史首次见证，也不立即训练。已预注册V5前瞻只追加账本 `artifacts/announcement_body_v5/protocol.json`：把2017–2025旧数据永久标为archive_reconstructed，把冻结后真实新公告的查询响应哈希和首次见证时间单独保存；至少观察到一份冻结后新公告前，prospective_pit_verified仍为false。正式模型继续V6。

V5账本实现、追加幂等、哈希变化留证、上海时区和禁止回填测试已经完成并冻结，锁 `21ceafd48929d82d033f6a2753e7352b2e551ae3cd4cb7cea07bf7c17254be79`。首次官方观察发现未分区接口声称6776条、226页，但第100页后重复第一页；因此仅捕获3000个旧公告ID，全部标记为archive_reconstructed并保留为接口截断证据，`prospective_new_eligible=0`，不能用于训练。

V5r1按冻结的2026-06-30沪深300快照300只证券及官方orgId逐股分区，避免100页上限，锁 `f88470fab5b62a2e403f5f5fb987d4a71cf4abeb232707deb47169445262f530`。首次运行在任何账本写入前因零结果分区返回`announcements=null`而失败，原失败证据保留。V5r2只修复“totalAnnouncement=0时允许null为空列表”，同时将访问改为单线程、固定0.2秒间隔、零重试；11项父链/修复测试通过，冻结锁 `2e042f115b5b91eebd10322fd49b38743f9e18381145723abdaf752351385c0c`。

V5r2首次观察曾在任何账本写入前被官方接口以HTTP 403拒绝，失败证据保留。用户随后明确要求直接测试；15:08上海时间在冻结锁完整且无重复进程后执行一次，接口没有再次403，并于15:10完整结束：冻结沪深300观察池300只、300个分区页、官方报告587条、返回587条、唯一587条，分页聚合完全一致。587条均为冻结当日基线记录，`prospective_new_eligible=0`，不能冒充冻结后样本。V5r2锁仍完整，`prospective_pit_verified=false`、`historical_pit_verified=false`、`model_training_ready=false`、`execution_authorized=false`。当天不再重复请求；下一上海日期最多观察一次，只有真实冻结后新公告才进入前瞻账本。今天是周六，不运行每日影子观察，126日不增加。正式模型继续V6。

用户要求继续强化模型后，已直接建立并冻结V24准备线，而不是等待或重跑旧历史组合。V24把公告正文87条已批准事实转成保留“本次/前次/上年同期”角色的结构化特征，加入指引修正幅度、当期相对同期、合同收入占比、回购进度和不确定性特征；固定只允许下一交易日入场，并同时要求5日和20日标签成熟。15项父链/新测试通过，冻结锁 `AE8CCF2F0A70D2916762B43AD335EF8F4765EF1D667EC0003C0365F35B222CAE` 完整。一次冻结评估生成11份金标文档/87条事实的特征预览；因真实冻结后事件仍为0，训练门禁正确拒绝，未读取历史收益、未拟合或回测。下一步由V5r2每日追加真实新公告，再为合格公告抓取协议批准正文并等待固定5/20日标签成熟；门禁满足前不训练，正式模型继续V6。

V24前瞻正文抓取链也已直接实现、测试、冻结和执行。它只接受V5r2中`prospective_metadata_eligible=true`且身份未变化、非修订隔离的新事件；逐份核对巨潮详情身份和官方PDF身份，PDF/详情/解析结果均哈希存证，扫描件或低质量文本按PDF规则隔离且不启用OCR，失败不自动重试。19项V24/V5父链测试通过，锁 `9FE65EEFB4D13AE9D3BE7DF8851E86374BC912A972E0EB13C59278EC2433A072` 完整。当前执行结果为合格新事件0、抓取0、隔离0，属于成功空运行；后续有新事件时自动执行同一冻结链，不改代码或等人工继续。

继续检查发现上述冻结正文链错误地把未来反复运行设计成一次性`run.started.json/report.json`，新事件到来后会因拒绝覆盖而失败。原锁和空运行证据保留；已新建V24正文r1，只将运行收据改为不可覆盖的`runs/<attempt>.started/report/failure.json`，动态状态单独原子替换，选择、下载、哈希和隔离规则完全不变。21项父链/修复测试通过，锁 `4764F6E33405B44E6F17C43DFFBEF3AC2AEB88B032741B58F79A3769CFDDBD96` 完整；冻结后连续执行两次均成功，各自产生独立收据且没有网络请求或覆盖。后续自动任务使用r1，不再调用一次性父版。

用户指出模型强化主线可能跑偏，判断成立：V24提高了未来数据可信度和可用性，但截至当前没有前瞻事件，尚未改变任何预测分数或V6性能。V24改为后台每日数据线，不再占用主研究线。已预注册V25单一模型假设：删除历史表现为负的两阶段尾部目标，使用现有PIT V10特征分别预测5日与20日行业内中性收益排名，固定0.5/0.5行业中性分位合成，不搜索权重、top-N或参数；组合、成本、日历和代理基准完全沿用已验收V20r2。协议 `artifacts/research_v25/protocol.json`。下一步直接实现模型/防泄漏/控制复现测试，测试冻结后只运行一次六年滚动验证；结果仍属于回顾性证据，未完成126日未来前不替换V6。

代码审计在实现前发现上述V25与V10已完成的5日/20日Ridge+LightGBM多周期模型重复，V11/V12也已经覆盖列表排序尾部目标；因此V25在未冻结、未训练、未读取新结果时取消，不浪费一次验证机会。主线修订为V25r1单变量假设：保持冻结V10/V16分数中的V6、Ridge、5/20权重和全部V20r2组合规则，仅把原8年LightGBM贡献替换为固定8/5/3年时间窗口等权集成，以降低单窗口预测方差。PIT父链复核显示历史成分每期300只、行情799只且2010起、财务PIT违规0、行业未来日期违规0；V20r2和V21锁完整。V25r1及控制回放共13项测试通过，尚未冻结或运行性能。下一步冻结全部代码、父分数、账本和原始输入哈希，随后唯一启动一次六年同口径验证。

V25r1已完成并验收为有效负结果：控制73期、21898行持仓、1533行NAV和2笔公司行动精确复现，锁及5份输出哈希完整。候选IC5/IC20为+0.03049/+0.03753，但累计超额-6.77个百分点，仅3/6年度正，科技IC -0.01264，最大回撤-40.68%，换手和成本也略高；所有替换关键门槛失败，保持V6。没有搜索8/5/3窗口权重。当前已预注册并实现V26单变量目标变更：只把8年LightGBM的L1残差幅度目标替换成同一残差是否为正的二分类概率，冻结V6/Ridge/周期权重和V20r2组合规则完全不动；17项测试已通过，尚未冻结或运行V26性能。下一步冻结并唯一运行V26。

V26已完成并验收为有效负结果：锁`B875B51B6C75BA062440232BF8310AD8D0C5236559A0105F3483B6358272C9D1`、5份输出哈希和V16控制复现完整。候选累计超额-2.28个百分点、3/6年度正；IC5/IC20 +0.02920/+0.03412，科技IC -0.01127，回撤-40.30%，换手和成本高于控制。相比V25r1损失收窄但仍不能替换V6。下一轮只预注册V27科技方向概率专家：仅对PIT科技行业股票使用固定60%全市场+40%科技专属概率，非科技分数逐位不变；不搜索权重/参数/子行业。冻结评价轨迹有92只科技股、3594条科技记录、73日期，行业未来日期违规0。为避免同一轮连续历史试验，本轮只冻结协议，下一自动轮再实现测试、冻结并唯一运行。

用户随后明确要求执行更广的体系重构，因此未实现、未冻结、未运行的V27已标记被V28取代。V28已实现三层独立门禁：全体排序、行业中性头部选择、组合转换；组合层使用策略/基准财富比的主动回撤，固定门槛-10%，绝对-18%不再用于V28及以后，但V25/V26失败结论禁止追溯改写。模型使用5日/20日方向概率与行业日期内top20%尾部概率固定50/50混合，两个严格先前年份交叉拟合产生0/0.25/0.5/1置信度，仅缩放15%主动预算。数据准入审计拒绝覆盖不足的研报（268只）、北向持仓（199只且2024停止）、缺历史first_seen/修订链的公告；没有新特征冒充通过。V28及父链23项测试通过。V28已冻结，锁 `C163864C4C9B24E2C5E4A176519BA9C440B7DF338F1A83C65989BE389AA4BF05`，并于2026-08-29 19:31上海时间唯一启动；启动器PID47780、实际Python工作PID43080已核对命令行与创建时间，当前阶段为`loading_frozen_pit_dataset`。日志位于`artifacts/research_v28/logs`，实时页硬链接为`artifacts/research_v28/run_stdout.log`和`run_stderr.log`。运行期间禁止重复启动或修改冻结输入；完成后先核验V16控制账本、输出哈希和锁完整性，再独立判定排序、头部选择、组合转换三层门禁。正式模型仍为V6。

V28于19:48完成并验收为有效负结果：IC5/IC20为+0.01928/+0.02392，但年度稳定性仅3/6且科技IC=-0.01414；行业中性头部5/20日价差均为正、精度提升+3.09个百分点，但5日价差也仅3/6年度为正；累计超额-2.55个百分点，仅1/6年度正。风险转换本身通过：主动回撤-2.36%、跟踪误差0.39%、换手与成本低于控制。保持V6。

当前主线为V29单变量目标对齐：V28结果出来前的代码审查已识别尾部训练标签按全市场日期定义，而选择门禁按行业内头部评价。V29只把尾部标签改为PIT日期×宽行业内top20%；方向概率、回归锚、全部特征与数据准入、8年窗口、0.3/0.7周期、50/50方向尾部权重、交叉拟合置信度、组合规则和三层门禁均不变。V29及父链27项测试通过，冻结锁`2FDC0246165EEC002F09E8768CC1A2E8EA2F76242CAA4F3D7D21F94E0CF3C2CF`完整；2026-08-29 19:53上海时间唯一启动，启动器PID44100、实际Python工作PID41244已核对，当前处于`loading_frozen_pit_dataset`。运行期间禁止重复启动或修改冻结输入；完成后按原三层门禁验收，禁止搜索分位数、权重、行业、科技覆盖、top-N或符号。

独立公告正文数据试验已完成并冻结：从966,865条标题元数据按不读取收益的固定规则选12份官方PDF，全部下载，11份文本提取通过、1份扫描件隔离；共34页、12张表、103处未绑定数字候选。四份代表样本已渲染核对，发现修正预告会同时含前次/本次/上年同期，数值列绑定未批准。因此正文试验锁 `ee9d5696f77da8b9659be2e4d93b2e56094f50f2465b55b4bb7c72204f0afac6` 保持model_training_ready=false，不能进入V22。11项数据链测试通过。

## 已完成任务：V21分项诊断（不是新生产预测模型）

V20r2已于2026-08-29 05:51:32完成全部六年73期。独立输出验收已通过：父链冻结完整、四份最终输出与六份年度checkpoint哈希正确、净值/成本复算一致、7笔换股结算元数据一致。验收文件为 `artifacts/autopilot/v20r2_acceptance_20260829.json`；这不是独立交易重放或模型性能通过。

结果：对照累计收益35.59%、最大回撤-40.67%；自适应33.08%、-41.01%；择时13.49%、-23.24%。三组科技IC均为负，均不能替换V6。基准是PIT快照代理组合，不是官方沪深300；旧V6/V8归档不可直接比较。现有3/126天影子属于lowvol_top20，也不能算作V20r2或V21未来验证。

下一项已实施为 `research_v21`：只复现原模型，保存逐年逐股组件分数、原生/5日目标、文本覆盖和过去验证门禁。固定7组件×4目标×4分组，按同一73个日期报告IC；要求每期V16对照及自适应IC5/IC20/科技IC复现父版，不一致就失败。禁止修改权重、反转信号、选择赢家或宣称通过。9项诊断测试、5项验收测试及37项既有针对性回归共51项通过；全项目测试本轮未重跑。

优先查 `artifacts/active_research.json`、`artifacts/research_v21/runtime_status.json` 及真实进程；不再重启V20/V20r1/V20r2。V21 `scores_YYYY.csv` 和 `diagnostics_YYYY.json` 是排他写入的逐年结果，不能当作完成报告。使用 `.\.venv311\Scripts\python.exe -B -m research_v21.cli verify` 验证；`freeze`/`run` 禁止重复覆盖。完成后校验父IC复现、输出哈希、各组件/科技/文本覆盖/目标口径及验证门禁，再决定一项有证据的新假设；若无支持证据，保留负结果而非无限调参。长任务保持单实例，由原半小时自动任务接续。

V21于06:02上海时间启动，锁为 `569D3ADB9E9950604E412D362D1A2CDC351BE354EFCFB4D06919F668D9CDAB85`；实际工作PID4976已核对命令行和创建时间，处于loading_dataset。实时日志API已确认读取V21，正式V6未改变。PID仅为本次记录，下次仍须实时核验。

## 已完成修订：V20r2（历史过程保留）

优先检查 `artifacts/active_research.json` 和 `artifacts/research_v20r2/runtime_status.json`；V20、V20r1均保留失败证据，禁止重启或修改旧锁。V20r2补充600837与601989换股事件、原始价格锚点、公共交易日日历NAV、停牌持仓保留和逐年checkpoint，未改变预测参数或原始训练数据。预检799股、73期通过；新21项测试通过，研究回归150/151通过（仍为V18冻结夹具错误）。

V20r2报告是独立记账口径的回顾性研究，不能与旧版归档收益直接比较，不代表替换批准。查看 `checkpoints/through_YYYY.json` 可确认完整年度中间结果；部分结果不等于最终通过。未来自动检查若确认V20r2正在运行，只监测、不并行重启；完成后按完整性、结算/现金账、性能和可比性顺序验收。未经新的明确假设不能继续无限刷版本。

入口：`.\.venv311\Scripts\python.exe -B -m research_v20r2.cli verify`。`freeze`、`run`均排他写入，禁止重复执行；日志位于 `artifacts/research_v20r2/run_stdout.log` 与 `run_stderr.log`。

## 前次修订：V20r1（保留历史过程）

V20在2026-08-29 04:21:29上海时间构建历史数据时，额外申请2.38GiB连续浮点数组失败，未开始训练。原错误与锁保留。正在执行V20r1：使用Pandas copy-on-write减少隐式整块复制，不改变行数、股票池、年份、精度或模型参数。后续检查应优先读取 `artifacts/research_v20r1/runtime_status.json`（生成后），而不是重启V20。

V20r1命令入口：`.\.venv311\Scripts\python.exe -B -m research_v20r1.cli freeze|run|verify`，必须先测试，且freeze/run不得重复覆盖。详细决策见 `RESEARCH_DECISIONS.md` 第002、003条。

V20r1已通过3项内存/一致性测试，copy-on-write下研究版本回归121/122通过（仍只有V18旧夹具错误）。已冻结并后台重跑，锁：`59116CFC7A664AFEBD6BCF6CD51DEC7785BAF8CFE7FD340D060FCF0B979205AA`。用 `artifacts/research_v20r1/runtime_status.json` 查实际工作进程，勿再次运行启动脚本。

2026-08-29 04:33再次执行：进程身份核验通过，仍在loading_dataset，CPU持续增长，无新异常。工作集约7.93GB、私有内存约15.83GB，继续关注峰值。当前网页研究候选由 `artifacts/active_research.json` 指向V20r1；状态层核验进程身份，退出或状态不完整会提示异常，不会自动重启。新增状态测试10/10、原V20调用链10/10和网页AppTest通过，父锁未改，正式V6未切换。

## 原 V20 方案（保留）

独立修复 V17 同期未来收益择时和 V19 缺少 V16 配置字段的问题。

三组固定实验：V16 同实现对照、状态自适应权重、历史市场动量择时。市场状态只读截至决策日收盘的行情与滞后一期的 PIT 成分权重；次日才执行。市场代理不是官方指数。

旧 V16 清单仍是 2026-08-21 选股加 2026-08-28 择时，已在新的研究状态入口隔离，不伪造当天预测。正式网页仍使用 V6。

状态文件：`artifacts/research_v20/runtime_status.json`；完成报告：`artifacts/research_v20/report.json`。

运行命令（项目根目录）：

```powershell
.\.venv311\Scripts\python.exe -B -m unittest discover -s tests -p test_research_v20.py -v
.\.venv311\Scripts\python.exe -B -m research_v20.cli freeze
.\.venv311\Scripts\python.exe -B -m research_v20.cli run
.\.venv311\Scripts\python.exe -B -m research_v20.cli verify
.\.venv311\Scripts\python.exe -B -m research_status
```

`freeze` 和 `run` 禁止重复覆盖。若存在启动记录但进程已停止，先查异常，不删除启动记录重新来过。

## 原后续顺序（当前先完成上面的V21诊断）

1. 完成真实调用链测试、旧冻结输入校验，并运行 V20；保留负结果。
2. 审核 V20 成本、停牌/跌停退出和持仓漂移的近似记账，不能把继承的近似回测说成可成交实盘收益。
3. 在独立数据目录构建同日行情、公告、财务、行业暴露的 PIT 更新链；先解决复权口径及来源可追溯，再发布新的研究快照。
4. 每次只预注册一项具有证据支持的新假设；先通过逻辑测试再冻结。
5. 比较原始门槛：正累计超额、至少4/6正超额年、同口径IC不低于V6、科技IC非负、最大回撤不差于-18%、同口径换手和成本不高于V8、126个新未来影子交易日有效。没有可比证据的项视为未通过。

## 本次记录

- V20 的10项针对性测试通过；研究版本回归共119项，118通过、1项V18历史测试夹具错误。
- V18失败用例手工构造了空回归器列表，且四个公告间隔14日，却在第二个公告公开前断言其排名。不是已拟合模型的真实调用条件。保留被冻结的原测试，另在 `tests/test_research_archive_contracts.py` 用合法三头回归器和同日可见公告验证原意；不能声称原套件全绿。
- 原V16父锁校验通过。V20尚未宣称性能通过。
- 不再将 V17 原回测的94%胜率作为推荐依据。
- 自动任务 `a` 最初更新为工作日18:30影子观察后继续研究；后按用户进一步授权改为上面的半小时闭环，影子观察仍每日一次，未创建重复自动任务。
- 全量回归：141项中140通过、1项上述V18冻结夹具报错；新增合法用例通过。Streamlit AppTest异常数0。
- V20已冻结：`FA37037684B83A30C4456DB7F303D3667EBBA81AB81A68494BE4A0CEDCC70960`。冻结校验通过，`execution_authorized=false`。
- 2026-08-29 04:18上海时间，完整回测已在隐藏后台启动。启动器PID8012，实际Python工作进程PID25936（以运行状态与实时进程校验为准，PID不能长期当作唯一身份）。启动器：`scripts/start_v20.ps1`。
- 当前仍在构建历史数据集，尚无收益结论。下次先检查 `runtime_status.json` 和日志，不能因加载较久而重复启动。
- 原未来影子状态只读检查：3/126日，最新行情2026-08-26、300只、1份信号；`frozen_inputs_intact=true`、`execution_authorized=false`。本轮未补写影子数据，原每日任务后续按规则推进。
